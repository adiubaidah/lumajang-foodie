import {
  useEffect,
  useState,
  createContext,
  ReactNode,
  useContext,
} from "react";

const LoadingContext = createContext(false);

export const LoadingProvider = ({children}: {children: ReactNode}) => {
    
}
